# AI Team OS cross-platform production build input.
# This file is a reviewed template; Flutter does not load it automatically.
# Replace the example domain in the protected CI/CD environment and pass the
# value with --dart-define to Android, iOS, Windows, and macOS release builds.

TEAM_OS_BASE_URL="https://team-os.example.com"
TEAM_OS_ALLOW_INSECURE_LOCAL="false"
