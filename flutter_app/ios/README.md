iOS runner placeholder.

Generate the real Flutter iOS project on macOS after installing Flutter and Xcode:

```bash
cd flutter_app
flutter create . --platforms=ios
flutter build ios --release
```

This directory only reserves the native iOS app location for the Flutter client.
